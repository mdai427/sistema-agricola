[build]
builder = "nixpacks"

[deploy]
startCommand = "cd server && npm install && npx prisma generate && npx prisma db push && node src/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
